# OpenAI / Groq / 其他兼容 API
# 使用 Groq 时：OPENAI_API_BASE=https://api.groq.com/openai/v1
OPENAI_API_KEY = 'sk-xxx'
OPENAI_API_BASE =
MODEL_NAME = 'gpt-3.5-turbo'

# Groq API (免费额度：20 RPM, 14400 请求/天)
# 如果使用 Groq，取消下面的注释并填写你的 key
# GROQ_API_KEY = 'gsk_xxx'
# GROQ_MODEL = 'llama-3.3-70b-versatile'

HF_TOKEN = 'hf_xxx'

# 火山引擎
BYTEDANCE_APPID =
BYTEDANCE_ACCESS_TOKEN =

# 如果在从 huggingface 下载模型时报错，uncomment 下面的代码
# HF_ENDPOINT = 'https://hf-mirror.com'

# Bilibili 上传凭据
BILI_SESSDATA =
BILI_BILI_JCT =

# 代理设置（用于访问 YouTube）
# 如果你需要代理才能访问 YouTube，请取消下面的注释并修改为你的代理地址
# HTTP_PROXY=http://127.0.0.1:10808
# HTTPS_PROXY=http://127.0.0.1:10808

# 绕过代理的域名（用于解决 Bilibili 上传问题）
# 如果你配置了代理但 Bilibili 上传失败，请取消下面的注释
# NO_PROXY=api.bilibili.com,member.bilibili.com

# ========== 视频合成配置 ==========

# 视频编码器选择：
# - auto: 自动检测（优先使用 NVENC 硬件编码，需要 NVIDIA GPU）
# - nvenc: 强制使用 NVIDIA NVENC 硬件编码
# - x264: 使用 libx264 软件编码
VIDEO_ENCODER=auto

# 视频质量设置（仅当使用 x264 时生效）：
# - high: 高质量（CRF 18，慢速预设，文件较大）
# - medium: 中等质量（CRF 21，中等预设，平衡）
# - low: 低质量（CRF 25，快速预设，文件较小）
VIDEO_QUALITY=high

# 说明：
# 1. 如果你有 NVIDIA GPU，推荐使用 auto 或 nvenc，编码速度快 5-10 倍
# 2. 如果追求最佳画质且不在乎时间，使用 x264 + high
# 3. 如果存储空间有限，使用 low 质量